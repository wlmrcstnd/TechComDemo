import { LightningElement } from 'lwc';

export default class FaqAccordionNew extends LightningElement {
    // No JavaScript needed for the accordion behavior.
    // The shared `name` attribute + API 67.0 handle it natively.
}
